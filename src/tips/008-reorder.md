## Reorder Cards

Press **J** to move the selected task down or **K** to move it up within
its column. For epic subtasks, the order here is the auto-dispatch order —
the next task dispatched by an agent will be whichever backlog subtask
sits highest in the column.
